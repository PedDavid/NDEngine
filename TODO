"Window" GLFW JoyStick (seems to be bugged or doesn't recognize mine)
Window Manager, mostly everything...
Our Window class is mostly a OO wrapper, maybe ditch it for Window Manager?

c# tools (shader editor) // Not important (we hot swap shaders now boys!)

IMPORTANT: DECENT MULTITHREAD MECHANISM FOR FILE WATCHER, IT WAS JUST A TEST 
(update: not multithreaded now because we had to check in current thread because of opengl anyway)

FreeImage as static library, can't compile because of vs 2015 errors

Math: Correct matrix * overload; vector swizzling and vector variable constructors

PBR: Global Illumination (later), everything else, optimization

Font rendering

Vertex with multiple normals in obj currently gets the last normal, should average/half vector